# Copy to .env for local development (`cp .env.example .env`).
# Matches the credentials in docker-compose.yml.
DATABASE_URL=postgresql://debt:debt@localhost:5433/debt

# HTTP server (optional overrides)
PORT=3001
HOST=0.0.0.0
